// Centralized list of files to ignore during file selection and processing
// All entries should be lowercase for case-insensitive comparison
export const IGNORED_FILES = new Set([
    "package-lock.json",
    ".ds_store"
]); 